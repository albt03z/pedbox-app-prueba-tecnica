/** Forma de respuesta estándar para todos los endpoints de listado paginado. */
export interface PaginatedResult<T> {
  data: T[];
  meta: {
    total: number;
    page: number;
    limit: number;
    totalPages: number;
  };
}
